<div class="auth">
  <h2 class="auth__title">{{title}}</h2>
  <form class="auth-form">
    {{#each input}}
      <div class="auth-form__field">
        <label class="auth-form__label" for={{id}}>{{label}}</label>
        <input class="auth-form__input" id={{id}} type={{type}} name={{id}} placeholder={{placeholder}}>
      </div>
    {{/each}}
  </form>
</div>
